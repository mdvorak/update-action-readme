name: "TEST"
description: "Test."

inputs:
  foo:
    description: "Foo"
    required: true
    default: abcd
  bar:
    description: |
      Multi-line
      Bar.
    required: false
outputs:
  goo:
    description: |
      Multi-line
      Goo.
    value: ""

runs:
  using: composite
  steps: [ ]
